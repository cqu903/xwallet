<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zerofinance.xwallet.repository.CustomerMapper">

    <resultMap id="CustomerResultMap" type="com.zerofinance.xwallet.model.entity.Customer">
        <id property="id" column="id"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="nickname" column="nickname"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findById" resultMap="CustomerResultMap">
        SELECT id, email, password, nickname, status, created_at, updated_at
        FROM customer
        WHERE id = #{id}
        LIMIT 1
    </select>

    <select id="findByEmail" resultMap="CustomerResultMap">
        SELECT id, email, password, nickname, status, created_at, updated_at
        FROM customer
        WHERE email = #{email}
    </select>

    <select id="findActiveByEmail" resultMap="CustomerResultMap">
        SELECT id, email, password, nickname, status, created_at, updated_at
        FROM customer
        WHERE email = #{email}
          AND status = 1
    </select>

    <insert id="insert" parameterType="com.zerofinance.xwallet.model.entity.Customer" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO customer (email, password, nickname, status, created_at, updated_at)
        VALUES (#{email}, #{password}, #{nickname}, #{status}, #{createdAt}, #{updatedAt})
    </insert>

</mapper>
